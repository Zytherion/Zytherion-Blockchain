[package]
name = "tfhe-cgo"
version = "0.1.0"
edition = "2021"

[lib]
name = "tfhe_cgo"
crate-type = ["staticlib"]

[dependencies]
tfhe = { version = "0.7", features = ["integer", "x86_64-unix"] }
bincode = "1"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1